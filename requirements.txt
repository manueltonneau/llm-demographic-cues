# Python dependencies for the analysis scripts.
# Pinned to the versions used to produce the released results; nearby versions
# should also work. Install with:  pip install -r requirements.txt
pandas==2.2.3
numpy==1.26.4
scipy==1.11.4
statsmodels==0.14.4
matplotlib==3.6.3
textstat==0.7.13
vaderSentiment==3.3.2
spacy==3.8.14
pyarrow==19.0.1            # parquet I/O backend for pandas

# spaCy English model used by scripts/appendix/task2_features.py (dependency depth).
# Install separately after the packages above:
#   python -m spacy download en_core_web_sm

# Only needed for the Appendix B.3 run-to-run variance harness
# (scripts/appendix/task_gpt_variance_build.py / task_gpt_variance_submit.py),
# the only scripts here that call a model API:
#   pip install openai tiktoken
